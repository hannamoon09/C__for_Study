// 초깃값을 1부터 시작하기.c
#include <stdio.h>

int main() {
    for (int i = 1; i <= 100; i++) { // 1부터 100까지 증가하면서 100번 반복
        printf("Hello World %d\n", i); // Hello, World!와 i의 값을 함께 반복
    }

    return 0;
}