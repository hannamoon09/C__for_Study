// 상수.c
#include <stdio.h>

int main() {
    const int con1 = 10;
    // con1 -> 상수
    // 10 -> 리터럴

    /*
        상수는 변하지 않는 값을 뜻함
        변수는 한 번 선언해도 계속 값을 바꿔줄 수 있지만 상수는 X
        리터럴(literal)은 "문자 그대로" 라는 뜻인데 C언어에서는 값 그 자체를 뜻함
    */
}